import Foundation
import XCTest
import SwiftMetric
import SourceKittenFramework

class CyclomaticComplexityTests: XCTestCase {

    override func setUp() {
        continueAfterFailure = false
    }

    func testMeasure() {
//        let file = File(contents: "func a() {\n if 0 == 0 {\n print(true)\n} \n }")
//        let structure = Structure(file: file)
//        
//        let result = CyclomaticComplexity().measure(dictionary: structure.dictionary)
//        XCTAssertEqual(result, 1)
    }
    
}
